add_executable(unit-test unit-test.cpp)
target_link_libraries(unit-test random-graphics)

if (OPENGL_FOUND)
    if (Qt6Widgets_FOUND)
        add_executable(gldemo gldemo.cpp)
        target_link_libraries(gldemo random-graphics Qt::Widgets)
    elseif (Qt5Widgets_FOUND)
        add_executable(gldemo gldemo.cpp)
        target_link_libraries(gldemo random-graphics Qt5::Widgets)
    endif()
endif()

if (TARGET gldemo)
    if (UNIX)
        target_link_libraries(gldemo random-graphics dl)
    endif()
endif()
